---
- name: Create tags
  hosts: all
  become: yes
  tasks:
    - name: Copy content with alpha tags
      copy:
        content: "Hello world with alpha"
        dest: /tmp/alpha.txt
      tags: [never, alpha]
    - name: Copy content with beta tags
      copy:
        content: "Hello world with beta"
        dest: /tmp/beta.txt
      tags: [never, beta] 
